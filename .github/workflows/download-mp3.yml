name: Download Daily MP3
on:
  schedule:
    - cron: '50 12 * * *'   # UTC 12:50 = 台灣 20:50
  workflow_dispatch:

jobs:
  download:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Create mp3 folder
        run: mkdir -p mp3

      - name: Generate date with cross-day check
        id: date
        run: |
          CURRENT_HOUR=$(TZ='Asia/Taipei' date +'%H')
          echo "Current Taiwan hour: $CURRENT_HOUR"
          
          if [ "$CURRENT_HOUR" -lt 6 ]; then
            # 若在凌晨執行（0:00 ~ 5:59），使用「今天」的日期
            # 因為排程延遲可能讓 workflow 跨日執行
            DATE=$(TZ='Asia/Taipei' date +'%Y.%m.%d')
            echo "⚠️ 凌晨執行，使用今天日期: $DATE"
          else
            # 其他時間（6:00 ~ 23:59），使用「明天」的日期
            DATE=$(TZ='Asia/Taipei' date -d 'tomorrow' +'%Y.%m.%d')
            echo "✅ 正常時段執行，使用明天日期: $DATE"
          fi
          
          echo "DATE=$DATE" >> $GITHUB_OUTPUT

      - name: Download MP3
        run: |
          FILE=G-AwakerHeart${{ steps.date.outputs.DATE }}.mp3
          URL=http://streamak0119.akamaized.net/GoodnewsMedia/$FILE
          echo "FILE=$FILE" >> $GITHUB_ENV
          echo "Downloading $URL"
          curl -f -L \
            -A "Mozilla/5.0" \
            --retry 3 \
            --retry-delay 60 \
            --retry-connrefused \
            -o "mp3/$FILE" \
            "$URL"

      - name: Verify download
        run: |
          FILE_PATH="mp3/${FILE}"
          if [ ! -s "$FILE_PATH" ]; then
            echo "❌ 檔案不存在或是空檔案: $FILE_PATH"
            exit 1
          fi
          echo "✅ 下載成功: $FILE_PATH"
          echo "Downloaded files:"
          ls -lh mp3/

      - name: Clean old files (keep latest 7)
        run: |
          cd mp3
          ls -1t *.mp3 2>/dev/null | tail -n +8 | xargs -r rm -f
          echo "Remaining files:"
          ls -lh

      - name: Commit and push
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add mp3/
          git diff --cached --quiet && echo "No changes" || git commit -m "Update MP3 files: ${FILE}"
          git push origin HEAD:${GITHUB_REF#refs/heads/}
